{
  "name": "@moller/luau.ml",
  "private": true,
  "version": "2.0.0",
  "description": "The script sharing website.",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "test": "npx playwright test",
    "preview": "svelte-kit preview",
    "lint": "prettier --check --plugin-search-dir=. .",
    "format": "prettier --write --plugin-search-dir=. ."
  },
  "devDependencies": {
    "@playwright/test": "^1.15.0",
    "@sveltejs/adapter-cloudflare-workers": "^1.0.0-next.17",
    "@sveltejs/kit": "next",
    "patch-package": "^6.4.7",
    "prettier": "~2.2.1",
    "prettier-plugin-svelte": "^2.2.0",
    "svelte": "^3.34.0"
  },
  "type": "module",
  "dependencies": {
    "@supabase/supabase-js": "^1.21.0",
    "dedent": "^0.7.0"
  }
}
