{
  "name": "@moller/luau.ml",
  "private": true,
  "version": "2.0.0",
  "description": "The script sharing website.",
  "scripts": {
    "dev": "svelte-kit dev",
    "build": "svelte-kit build",
    "test": "npx playwright test",
    "preview": "svelte-kit preview",
    "lint": "prettier --check --plugin-search-dir=. .",
    "format": "prettier --write --plugin-search-dir=. .",
    "publish": "svelte-kit build && wrangler publish",
    "share": "svelte-kit build && wrangler publish -e dev"
  },
  "devDependencies": {
    "@sveltejs/adapter-cloudflare": "^1.0.0-next.3",
    "@sveltejs/adapter-cloudflare-workers": "^1.0.0-next.28",
    "@sveltejs/kit": "^1.0.0-next.216",
    "fluent-svelte": "^1.1.4",
    "prettier": "~2.2.1",
    "prettier-plugin-svelte": "^2.2.0",
    "svelte": "^3.44.0"
  },
  "type": "module",
  "dependencies": {
    "@fontsource/jetbrains-mono": "^4.5.0",
    "@supabase/supabase-js": "1.28.0",
    "dedent": "^0.7.0"
  }
}
